# 記述スタイル

## シェバン

```bash
#!/usr/bin/env bash
```

## クォート

変数展開は常にダブルクォートで囲む。

```bash
cp "$src" "$dst"
```

単語分割やグロブ展開を意図する場合だけ例外とし、その理由をコメントに残す。

```bash
# 意図的に単語分割させ、複数の引数として渡す
rsync $RSYNC_OPTS "$src" "$dst"
```

## 変数のスコープ

関数内のローカル変数には `local` を使う。

## フォーマット

shfmt を使う。
